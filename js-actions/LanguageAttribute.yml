name: 'Language Attribute'
description: 'Check for language attribute on HTML tag'
inputs:
  dom:  # dom to check
    description: 'What to check'
    required: true
    default: ''
runs:
  using: 'node14'
  main: 'lang.js'